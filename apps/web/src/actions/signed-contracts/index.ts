/** Actions do módulo `signed-contracts` — uma rota por arquivo. */
export { createSignedContract } from "./create-signed-contract.action";
export { listSignedContracts } from "./list-signed-contracts.action";
