/** Actions do módulo `addresses` — uma rota por arquivo. */
export {
  createAddress,
  type CreateAddressInput,
} from "./create-address.action";
export {
  listAddresses,
  type ListAddressesInput,
} from "./list-addresses.action";
